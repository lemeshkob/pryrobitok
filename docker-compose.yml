services:
  pryrobitok-bot:
    build: .
    container_name: pryrobitok-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      # edit phrases/*.txt and `docker compose restart` — no rebuild needed
      - ./phrases:/app/phrases:ro